#!/usr/bin/env -S guile -s
!#
;;; 磁盘安装 CLI 入口（阶段 2 实现，见 docs/storage.md 第 31 章）。
;;; 将提供 inspect / plan / apply 子命令。

(define (main args)
  (display "disk-install: 尚未实现（阶段 2）\n"))

(main (command-line))
