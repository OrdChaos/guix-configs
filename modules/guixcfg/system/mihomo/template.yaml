# Mihomo Phase 1 base config template（repository authority，
# docs/architecture/mihomo.md）。
#
# subscription URL 占位符由 mihomo-config materializer 在 boot 时替换，
# 合成结果写入 /run/mihomo/config.yaml（root 0600）经 -f 交给 Mihomo。
# 本模板【不可直接运行】——占位符不是合法 URL（materializer 的
# fail-closed 契约：占位符必须恰好出现一次，缺失/重复即失败）。
#
# Phase 1 边界：不接管 DNS（无 dns 段、dns-hijack 显式置空）、
# 不启用 fake-ip、不启用 self-update（binary 归 Guix）、controller
# 仅 loopback（secret 置空是单用户 loopback-only 部署的明确选择）。

mode: rule
log-level: info
# IPv6 关闭：机场节点全部是 v4（trojan），TUN 捕获 v6 流量映射给
# 节点后节点自身无 v6、连不上目标 v6 地址（bordeaux/github AAAA
# 秒断——VM 实测 "curl -6 bordeaux" 000/0.000s 而 -4 正常）。
# 关闭后系统 v6 流量走原生路径绕过 Mihomo（v4-only 机场的取舍）。
ipv6: false
external-controller: 127.0.0.1:9090
secret: ""
tun:
  enable: true
  stack: mixed
  device: mihomo0
  dns-hijack: []
  auto-route: true
  auto-redirect: true
  auto-detect-interface: true
proxy-providers:
  airport:
    type: http
    url: "@@MIHOMO_SUBSCRIPTION_URL@@"
    path: ./providers/airport.yaml
    interval: 3600
    # 订阅刷新走规则路由（MATCH→PROXY，经节点出口）：宿主侧网络对
    # 直连出站连接不稳定（实测 api.wd-purple.com 直连被 EOF 掐断、
    # 明文 53 被 fake-ip DNS 劫持），经节点才能拿到完整响应。不显式
    # 设 proxy=DIRECT——节点全挂时刷新失败可接受（provider 有
    # cache-first，换活节点后经 controller refresh API 恢复）。
    # Mihomo 原生维护 interval 调度、provider cache 与 refresh API。
    health-check:
      enable: true
      interval: 600
      lazy: true
      url: https://www.gstatic.com/generate_204
proxy-groups:
  - name: PROXY
    type: select
    use: [airport]
rules:
  - IP-CIDR,127.0.0.0/8,DIRECT,no-resolve
  - IP-CIDR,10.0.0.0/8,DIRECT,no-resolve
  - IP-CIDR,172.16.0.0/12,DIRECT,no-resolve
  - IP-CIDR,192.168.0.0/16,DIRECT,no-resolve
  - IP-CIDR,169.254.0.0/16,DIRECT,no-resolve
  - IP-CIDR,224.0.0.0/4,DIRECT,no-resolve
  - IP-CIDR6,fc00::/7,DIRECT,no-resolve
  - IP-CIDR6,fe80::/10,DIRECT,no-resolve
  - IP-CIDR6,::1/128,DIRECT,no-resolve
  # 刻意不为 SmartDNS 上游（223.5.5.5/119.29.29.29）加 DIRECT 规则：
  # 宿主侧/路由器对明文 53 端口全局劫持（fake-ip DNS，实测返回
  # 198.18.x A + fdfe:dcba:9876::x AAAA），直连永远拿到假 IP；
  # 唯一真实答案来自代理节点的出口 DNS。因此上游查询走 MATCH→PROXY
  # （节点死亡时 DNS 随之不可用——换活节点即可恢复）。
  - MATCH,PROXY
